// # Type
export type MAnimationType = {
  /**
   * **显示内容**
   * @type `boolean`
   * @default `true`
   * @description 当设置该值为 `true` 时，会显示内容
   * @description 当设置该值为 `false` 时，不会显示内容
   * @example
   * ```tsx
   * <MMessage show={true} />
   * ```
   * */
  show?: boolean;
};
