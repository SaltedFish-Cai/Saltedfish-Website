name: Deploy VitePress to Aliyun OSS

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Install dependencies
        run: |
          npm install
          # 补充你提到的平台依赖
          npm install @rollup/rollup-linux-x64-gnu

      - name: Build with VitePress
        run: npm run build:doc

      # --- 关键修改：同步到阿里云 OSS ---

      - name: Setup ossutil
        uses: manyuanrong/setup-ossutil@v2.0
        with:
          # 示例：oss-cn-shanghai.aliyuncs.com
          endpoint: ${{ secrets.ALIYUN_OSS_ENDPOINT }}
          access-key-id: ${{ secrets.ALIYUN_ACCESS_KEY_ID }}
          access-key-secret: ${{ secrets.ALIYUN_ACCESS_KEY_SECRET }}

      - name: Clean and Upload to Aliyun OSS
        run: |
          # 先删除 Bucket 下的所有内容（谨慎使用，确保 Bucket 仅用于此项目）
          ossutil rm oss://${{ secrets.ALIYUN_OSS_BUCKET }}/ -r -f
          # 再上传新内容
          ossutil cp -r .vitepress/dist/ oss://${{ secrets.ALIYUN_OSS_BUCKET }}/ -f

      # (可选) 如果你开启了阿里云 CDN，可以在这里增加一步刷新缓存
      # 需要在 RAM 权限中添加 AliyunCDNFullAccess
      - name: Refresh CDN Cache
        run: |
          # 假设你要刷新整个首页路径
          ossutil api purge-cdn-cache --domain-names your-domain.com --objects /
